import { LapToMilliPipe } from './lap-to-milli.pipe';

describe('LapToMilliPipe', () => {
  it('create an instance', () => {
    const pipe = new LapToMilliPipe();
    expect(pipe).toBeTruthy();
  });
});
